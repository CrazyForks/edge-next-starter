name: Deploy to Production

on:
  workflow_run:
    workflows: ['CI']
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

env:
  HEALTH_CHECK_RETRIES: 5
  HEALTH_CHECK_DELAY: 15

jobs:
  deploy-production:
    name: Deploy to Cloudflare Workers (Production)
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    environment:
      name: production
      url: ${{ vars.PRODUCTION_DEPLOYMENT_URL || 'https://cloudflare-worker-template-prod.workers.dev' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_branch || github.ref }}

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Build for production
        env:
          NODE_ENV: production
          CI: true
        run: pnpm run build

      - name: Run database migrations (wrangler.prod.toml)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: d1 migrations apply cloudflare-worker-template-prod --remote --config wrangler.prod.toml

      - name: Deploy to Cloudflare Workers (Production)
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy --config wrangler.prod.toml

      - name: Health Check
        id: health-check
        shell: bash
        run: |
          # Use configured URL, or fallback to Cloudflare default URL
          DEPLOYMENT_URL="${{ vars.PRODUCTION_DEPLOYMENT_URL }}"
          if [ -z "$DEPLOYMENT_URL" ]; then
            DEPLOYMENT_URL="https://cloudflare-worker-template-prod.workers.dev"
          fi
          HEALTH_ENDPOINT="${DEPLOYMENT_URL}/api/health"

          echo "🔍 Running health check on $HEALTH_ENDPOINT"

          for i in $(seq 1 ${{ env.HEALTH_CHECK_RETRIES }}); do
            echo "Attempt $i/${{ env.HEALTH_CHECK_RETRIES }}..."

            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$HEALTH_ENDPOINT" || echo "000")

            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ Health check passed! (HTTP $HTTP_STATUS)"
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi

            echo "⏳ Health check returned HTTP $HTTP_STATUS, retrying in ${{ env.HEALTH_CHECK_DELAY }}s..."
            sleep ${{ env.HEALTH_CHECK_DELAY }}
          done

          echo "❌ Health check failed after ${{ env.HEALTH_CHECK_RETRIES }} attempts"
          echo "status=failed" >> $GITHUB_OUTPUT
          exit 1

      - name: Alert on deployment failure
        if: failure()
        shell: bash
        run: |
          echo "🚨 PRODUCTION DEPLOYMENT FAILURE 🚨" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ⚠️ Critical: Production Health Check Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Immediate Actions Required:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Check Cloudflare Workers dashboard for deployment status" >> $GITHUB_STEP_SUMMARY
          echo "2. Review application logs for errors" >> $GITHUB_STEP_SUMMARY
          echo "3. Consider rolling back to previous deployment" >> $GITHUB_STEP_SUMMARY

      - name: Create deployment summary
        if: success()
        run: |
          echo "### 🚀 Production Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | **Production** |" >> $GITHUB_STEP_SUMMARY
          echo "| URL | ${{ vars.PRODUCTION_DEPLOYMENT_URL || 'https://cloudflare-worker-template-prod.workers.dev' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Commit | ${{ github.sha }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Branch | ${{ github.ref_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Health Check | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Deployed At | $(date -u +'%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
