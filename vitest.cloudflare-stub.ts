/**
 * Stub for `cloudflare:workers` built-in module.
 * Only used in vitest — provides a resolvable module path so Vite's
 * import resolver succeeds. The actual mock is applied via vi.mock()
 * in vitest.setup.ts.
 */
export const env = {};
